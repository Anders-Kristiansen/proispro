# Basher Orchestration — 2026-04-20T18-21-42Z

## Session

- **Topic:** collections-wishlist-forsale-implementation
- **Mode:** Background
- **Status:** Success ✓

## Work Completed

**Created Supabase migrations** for three new feature tables:

1. **001_collections.sql** — `collections` and `collection_discs` tables
   - Collections as named disc groupings (distinct from bags/round loadouts)
   - Junction table with sort_order support for user-defined ordering
   - RLS policies using indirect ownership via parent collection

2. **002_wishlist.sql** — `wishlist_items` table
   - Disc acquisition wishlist with priority (0/1/2) and weight range preferences
   - `acquired` flag preserves history; rows not deleted post-purchase
   - updated_at trigger for audit trail

3. **003_forsale.sql** — `forsale_listings` table
   - Marketplace listings with price/currency/status
   - Status as TEXT + CHECK constraint (no ENUM) for no-downtime constraint updates
   - Listed_at/sold_at timestamps track lifecycle

## Technical Details

- **Idempotency:** All migrations use `CREATE TABLE IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION`, and `DROP TRIGGER IF EXISTS` patterns
- **RLS Strategy:** Direct ownership (collections, wishlist_items, forsale_listings) and indirect ownership via junction table (collection_discs)
- **Schema Design:** Flat structure mirrors existing bags pattern; no denormalization on junction table

## Artifacts

- supabase/migrations/001_collections.sql
- supabase/migrations/002_wishlist.sql
- supabase/migrations/003_forsale.sql

## Learnings

RLS policy idempotency in Supabase: `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$` guards support DROP+CREATE pattern (PG 15/16 don't support CREATE POLICY IF NOT EXISTS).
