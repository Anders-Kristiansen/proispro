# Orchestration: Basher (Data Engineer)

**Timestamp:** 2026-04-16T20:42Z  
**Agent:** Basher (Data Engineer)  
**Event:** Disc photo storage — Supabase database migration

## Summary

Basher wrote `migration-v4-disc-photos.sql` implementing the disc photo feature in Supabase:
- Added `user_photo_url TEXT` column to `disc_wear_adjustments`
- Created four RLS policies on `storage.objects` for the `disc-photos` bucket  
  (INSERT / SELECT / UPDATE / DELETE scoped to `(storage.foldername(name))[1] = auth.uid()::text`)
- Created `v_user_disc_with_photo` view joining user discs with their photo URL
- Migration committed at `docs/migration-v4-disc-photos.sql`

## Decision Context

Storage path format used in migration: `{user_id}/{adjustment_id}.{ext}` — uses the `adjustment_id` (the `disc_wear_adjustments` row PK) as the file key. Column stores the **Storage path** (not a full URL); frontend resolves at render time via `supabase.storage.from('disc-photos').getPublicUrl(user_photo_url)`.

⚠️ **Path key discrepancy:** Danny's ADR specifies `user_disc_id` as the path key; this migration uses `adjustment_id`. Rusty's inbox decision (`copilot-disc-photo-adjustment-id.md`) tracks the fallback and the follow-up needed to align both.

## Status

✓ Migration SQL committed at `docs/migration-v4-disc-photos.sql`  
✓ Decision filed to `.squad/decisions/inbox/basher-photo-migration.md`  
